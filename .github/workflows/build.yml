name: Test Build
on: [push]

jobs:
  test:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '18'
    - run: echo "Testing connection..."
    - run: npm --version
    - run: node --version
